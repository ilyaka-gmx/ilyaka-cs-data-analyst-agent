# Core agent framework
langchain>=1.3.0
langgraph>=1.2.0
langgraph-checkpoint-sqlite==3.1.0
langchain-openai>=0.3.0
langchain-core>=0.3.0

# Data
pandas>=2.2.0

# Schemas and validation
pydantic>=2.0.0

# Semantic memory (mem0 with Qdrant in-process)
mem0ai>=2.0.2

# MCP server
fastmcp>=3.0.0

# Custom frontend API server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Environment
python-dotenv>=1.0.0

# UI: Streamlit
streamlit>=1.40.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# HTTP client (for connectivity tests)
httpx>=0.27.0

# Dataset auto-download (optional — only needed if CSV not in repo)
datasets>=3.0.0